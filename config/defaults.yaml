
# ==================================================== common =====================================================
common: &common
  mode: normal  # fast, normal, slow  ;default slow
  extractor:
    value: ""
    meta: {lang: en, version: 1}
  analyzer:
    value: ""
    meta: {lang: en, version: 1}
  collector:
    value: ""
    meta: {lang: en, version: 1}
  fast:
    recommender:
      value: |
        # CONTEXT #
        [{service_name} Category] The goal of this performance optimization is:
        Performance metric is {performance_metric.name}, which means: {performance_metric.value}, target is to improve by {slo_goal:.2%}.
        Current environment configuration:
        {static_profile}
        Historical tuning information (reference for improvements):
        {history_result}
        Current system load and performance analysis:
        {performance_analysis_report}
        Adjustable parameter set:
        {params_set_str}
        # OBJECTIVE #
        Current performance metrics do not meet expectations, please output a **parameter combination** directly based on the above information.
        1. Output must be a valid JSON object.
        2. Format as follows:
        {{
          parameter_name: value (if unsure about this value, this parameter set can be omitted)
        }}
        3. Only output key-value pairs, no explanations, comments, or extra text.
        4. Only include parameter adjustments that have positive impact on performance.
        5. Please output in English.
      meta: {lang: en, version: 1}
  normal:
    idea:
      value: |
        # CONTEXT # 
        The goal of this performance optimization is:
        Performance metric is {performance_metric.name}, which means: {performance_metric.value}, target is to improve by {slo_goal:.2%}
        Performance analysis report:
        {performance_analysis_report}
        Parameters you can analyze:
        {params_set_str}
        # OBJECTIVE #
        You are a professional system operations expert. Current performance metrics do not meet expectations. Please analyze potential tuning ideas based on the above performance analysis report.
        # Tone #
        You should maintain a serious, careful, and rigorous attitude as much as possible.
        # AUDIENCE #
        Your answer will be an important reference for other system operations experts, please provide your answer after careful consideration. Please output in English.
    recommender_positive:
      value: |
        [{service_name} Category] You are a professional system operations expert. Current performance metrics do not meet expectations. Please select adjustable parameter values based on the following tuning ideas, current environment configuration, and adjustable parameters.
        Please keep descriptions concise, focus on the key adjustment directions that need output, no need to summarize viewpoints, and do not output anything that has no impact on performance.
        Current environment configuration:
        {static_profile}
        Historical tuning information, historical tuning modified the following parameters, you can refer to this historical information to reflect on potential improvement points: 
        {history_result}
        Tuning ideas are:
        {optimization_idea}
        Parameters you can adjust are:
        {params_set_str}
        Please answer in JSON format, key is the adjustable parameter name, please provide adjustable parameters based on the above environment configuration information. If parameters are irrelevant, do not include them.
        value is the recommended value for the adjustable parameter, please provide reasonable specific values based on the above environment configuration information, carefully confirm whether each value can be used, avoid application startup failures after setting.
        Do not provide any response other than JSON, avoid adding comments. Please output in English.
    recommender_negative:
      value: |
        [{service_name} Category] You are a professional system operations expert. Current performance metrics do not meet expectations. Please select adjustable parameter values based on the following tuning ideas, current environment configuration, and adjustable parameters.
        Please keep descriptions concise, focus on the key adjustment directions that need output, no need to summarize viewpoints, and do not output anything that has no impact on performance.
        Current environment configuration:
        {static_profile}
        Historical tuning information, historical tuning modified the following parameters, you can refer to this historical information to reflect on potential improvement points: 
        {history_result}
        Tuning ideas are:
        {optimization_idea}
        Parameters you can adjust are:
        {params_set_str}
        Please answer in JSON format, key is the adjustable parameter name, please provide adjustable parameters based on the above environment configuration information. If parameters are irrelevant, do not include them.
        value is the recommended value for the adjustable parameter, please provide reasonable specific values based on the above environment configuration information, carefully confirm whether each value can be used, avoid application startup failures after setting.
        Do not provide any response other than JSON, avoid adding comments. Please output in English.
  slow:
    mem:
      value: |
        You are a memory summarization system, responsible for recording and preserving the complete interaction history between humans and the AI tuning expert. You will receive the expert’s tuning ideas and actual execution feedback from the past N steps. Your task is to generate a comprehensive summary of the tuning process, covering the expert’s tuning concepts and corresponding feedback details, ensuring there is no ambiguity. **The expert’s tuning ideas do not need to be fully preserved; only the most important core concepts and key feedback should be summarized.**
        ### Overall Structure:
        - **Overview (global metadata):**
          - **Task Objective: The tuning goal the expert is working to achieve.
          - **Progress Status: Current completion percentage, along with a summary of completed milestones or steps.
        - **Tuning Operation Sequence (numbered steps): Record only once per iteration.
        1. **Iteration i:**
            - Tuning Concept:
                The main idea of this tuning iteration, including bottleneck analysis, underutilized resources, etc. Must record highly specific optimization strategies for the given scenario.
            - Core Parameters:
                Core parameters related to the tuning concept. Record the parameters modified during tuning and their specific values.
            - Operation Result:
                The performance result of this iteration.
            - Key Findings:
                Important conclusions drawn from feedback, including parameter effectiveness and reflections on the tuning method. For discrete parameters, if their importance is confirmed, they should be fixed in the key findings; for continuous parameters, the direction of adjustment should be recorded.
        ### Example Template:
        ```
        ## Expert Tuning History Summary
        **Task Objective**: Increase MySQL QPS by 10% in the current environment.   
        **Progress Status**: 70% completed — achieved a 7% performance improvement.
        1.  **Iteration 1:**
            - Tuning Concept: Memory usage is far from the upper limit; adjust memory-related parameters to improve utilization.
            - Core Parameters: {{memory=50GB, xx=true}}
            - Operation Result: Performance result of this iteration is 60, improvement: 20.00%
            - Key Findings: Increasing memory settings was correct; further increasing memory-related parameters is beneficial.
        2. **Iteration 2:**
            - Tuning Concept: Increase shared_buffers to reduce memory cache misses.
            - Core Parameters: {{shared_buffers=20GB, maintenance_work_mem=2GB}}
            - Operation Result: Performance result of this iteration is 65, improvement: 30.00%
            - Key Findings: Increasing `shared_buffers` improved performance, but memory bottleneck has not yet been reached.
        ... (subsequent iterations continue to be recorded by number)
        ```
    recommender:
      value: |
        You are a system application optimization expert. Your task is to leverage your extensive experience in optimization to provide relevant recommendations.
        ### Overall Target:
        - The performance metric for the scenario is **{performance_metric.name}**, which signifies: {performance_metric.value}. The goal of tuning optimization is to achieve {slo_goal:.2%}.
        - The system performance analysis report is as follows:{performance_analysis_report}
        - The related parameters available for analysis and their descriptions are: {params_set_str}
        """ + "{long_mem}"  + "{short_mem}" + """
        ### Guidelines:
        - "Clear": Ensure that the answers are clear, concise, and directly address the question.
        - **Cautious, Lazy-Type Modification**: Only output parameter changes expected to yield good results; for continuous parameters, the adjustment range should be between 0~50%, and for discrete/enumerated parameters, avoid extreme adjustment spans; do not adjust parameters that are analyzed in the context as having "no impact" on performance.
        - **Logical**: Parameter adjustments should align with the tuning strategy, with each adjustment having a corresponding logic, and rigorously adjust mutually exclusive parameters simultaneously.
        - "Strict Formatting": Must meet type and unit requirements; the default unit for numerical values is "bytes". If a unit follows the numerical value, it should be represented as a string (e.g., "512MB"). Parameter output must be in **JSON format**.
        ### Example Template:
        ```
            {{
                "tuning_concept": "The main bottleneck in the current scenario is IO, but the system's memory usage is only about 30% of the current capacity. The current memory parameter is set at 20G, which is advisable to appropriately increase to enhance memory resource utilization and thereby accelerate performance ... (Additional tuning_concept)",
                "PARAM": {{
                "memory": "30GB",
                ... (Additional params)
                }}
            }}
        ```
      meta: {lang: en, version: 1}    
# ==================================================== nginx =====================================================
nginx:
  mode: normal  # fast, normal, slow  ;default slow
  extractor:
    value: ""
    meta: {lang: en, version: 1}
  analyzer:
    value: ""
    meta: {lang: en, version: 1}
  collector:
    value: ""
    meta: {lang: en, version: 1}
  fast:
    recommender:
      value: |
        # CONTEXT #
        [{service_name} Category] The goal of this performance optimization is:  
        Performance metric is {performance_metric.name}, which means: {performance_metric.value}, target is to improve by {slo_goal:.2%}.
        Current environment configuration:  
        {static_profile}  
        Historical tuning information (reference for improvements):  
        {history_result}
        Current system load and performance analysis:
        {performance_analysis_report}
        **Adjustable parameter set with value ranges and functional information**:
        {params_set_str}
        # REQUIREMENTS #
        Please output a "selection of parameters you believe will improve performance from the adjustable parameter set" directly based on the above information.
        **Strict requirements:**
        1. Output must be a valid simple JSON format string.
        2. Template reference format (uncertain key-value pairs can be omitted, do not repeat recommendations, JSON object should have proper bracket closure, do not introduce [ brackets), and avoid extra escape quotes (e.g., \"value\").
        {{
          "net.core.dev_weight": 128,
          "net.ipv4.tcp_keepalive_intvl": 60,
          "net.ipv4.tcp_keepalive_probes": 5,
          "net.ipv4.tcp_keepalive_time": 600,
          "net.ipv4.tcp_tw_reuse": 1,
          "net.ipv4.tcp_fin_timeout": 30,
          "net.ipv4.tcp_rmem": "4096 65536 16777216",
          "net.ipv4.tcp_wmem": "4096 65536 16777216",
          "net.ipv4.tcp_max_syn_backlog": 8192
        }}
        3. Only output JSON string in the correct format, no explanations, comments, or extra text. 
        4. Based on the above content, please provide a clean "simple" JSON parameter value recommendation that I can directly parse with json.loads.
        5. Please output in English.
      meta: {lang: en, version: 1}
  normal:
    idea:
      value: |
        # CONTEXT # 
        The goal of this performance optimization is:
        Performance metric is {performance_metric.name}, which means: {performance_metric.value}, target is to improve by {slo_goal:.2%}
        Performance analysis report:
        {performance_analysis_report}
        Parameters you can analyze:
        {params_set_str}
        # OBJECTIVE #
        You are a professional system operations expert. Current performance metrics do not meet expectations. Please analyze potential tuning ideas based on the above performance analysis report.
        # Tone #
        You should maintain a serious, careful, and rigorous attitude as much as possible.
        # AUDIENCE #
        Your answer will be an important reference for other system operations experts, please provide your answer after careful consideration. Please output in English.
      meta: {lang: en, version: 1}
    recommender_positive:
      value: |
        You are a professional system operations expert. Current performance metrics do not meet expectations, but the previous tuning round showed positive results (performance improvement or no degradation).
        Please "complete reasoning internally", only output the final JSON; do not output any text, code blocks, or comments other than JSON.

        Goal: Based on the following information, while maintaining the effective direction from the previous round, summarize parameter adjustment experience, further fine-tune parameters (moderately increase intensity within safe boundaries), only provide parameters that need changes and their recommended new values.

        Current environment configuration:
        {static_profile}

        Historical tuning information (including modified parameters and results):
        {history_result}

        Tuning ideas:
        {optimization_idea}

        Full set of adjustable parameters (with type/range/enum/default values, etc.) and baseline corresponding values:
        {params_set_str}

        Strict rules (must comply):
        1) Only output parameters that "need changes" compared to current configuration; do not output irrelevant or non-beneficial parameters.
        2) Prioritize small steps forward along the "previously effective" direction: continuous parameters increase by 100%~150% of the original step size (typically +10%~+30%), discrete/enum parameters take more aggressive but still safe adjacent levels; avoid one-time excessive changes (single parameter change amplitude should not exceed 2x or ±30%, whichever is stricter).
        3) Do not touch parameters proven to have "no impact" on performance; avoid adjusting obviously conflicting parameters simultaneously.
        4) Must satisfy dependency/mutual exclusion/upper-lower limit/type and unit requirements; numerical default unit is "bytes". If value has unit, represent as string (e.g., "512MB").
        5) Each parameter's recommended value must be actually acceptable by the system and ensure application can start.
        6) If no suitable changes, output empty JSON object.
        7) Please output in English.

        Output format (must strictly comply):
        - Only output one JSON object, key is "adjustable parameter name", value is "recommended value".
        - Do not output any extra text, explanations, examples, code fences, or comments.
    recommender_negative:
      value: |
        You are a professional system operations expert. Current performance metrics do not meet expectations, and the previous tuning round showed negative results (performance degradation/instability/errors, etc.).
        Please "complete reasoning internally", only output the final JSON; do not output any text, code blocks, or comments other than JSON.

        Goal: Based on the following information, summarize baseline, best tuning results, worst tuning results, and previous round tuning results and parameter values from historical tuning experience, reversely fine-tune parameters that may have caused degradation in the previous round, and select more conservative and safe values; only provide parameters that need changes and their recommended new values.

        Current environment configuration:
        {static_profile}

        Historical tuning information (including modified parameters and results):
        {history_result}

        Tuning ideas:
        {optimization_idea}

        Full set of adjustable parameters (with type/range/enum/default values, etc.) and baseline corresponding values:
        {params_set_str}

        Strict rules (must comply):
        1) Only output parameters that "need changes" compared to current configuration; do not output irrelevant or non-beneficial parameters.
        2) For parameters changed in the previous round and suspected of causing degradation: adjust in the "opposite direction" with small steps (amplitude 30%~50% of the previous step size, typically -10%~-20%); disable optional high-overhead features when necessary.
        3) Avoid adjusting too many parameters at once; do not adjust mutually exclusive parameters simultaneously; prioritize lower-risk correction solutions.
        4) Must satisfy dependency/mutual exclusion/upper-lower limit/type and unit requirements; numerical default unit is "bytes". If value has unit, represent as string (e.g., "1GB").
        5) Each parameter's recommended value must be actually acceptable by the system and ensure application can start.
        6) If no suitable changes, output empty JSON object.
        7) Please output in English.

        Output format (must strictly comply):
        - Only output one JSON object, key is "adjustable parameter name", value is "recommended value".
        - Do not output any extra text, explanations, examples, code fences, or comments.
  slow:
    mem:
      value: |
        - **Overview (Global Metadata):**
          - **Task Objective**: The specific performance goal for {application} (e.g., increase QPS, reduce latency, improve concurrency handling).
          - **Progress Status**: Current completion percentage, achieved improvements, and milestones reached.
        - **Sequential Tuning Actions (Numbered Steps):** Each iteration only record once.
        i. **Iteration i**:
            - Tuning Concept: 
                The main concept behind the tuning action, including identified bottlenecks (e.g., connection limits, CPU saturation, keep-alive inefficiency), underutilized resources, or scenario-specific strategies (e.g., static file serving, reverse proxy buffering).
            - Core Params:
                The key {application} configuration parameters modified in this round. ONLY the parameters that were actually changed in this iteration. Include exact parameter names and values (e.g., for nginx：worker_processes, worker_connections, keepalive_timeout, sendfile, tcp_nopush).
            - Operation Result:
                Quantitative performance outcome: e.g., QPS, P99 latency, error rate, CPU/memory usage before and after.
            - Key Findings:
                Insights derived from the results. For discrete parameters (e.g., on/off), fix their optimal value if confirmed. For continuous ones (e.g., buffer sizes), indicate the adjustment direction (increase/decrease). Note any side effects (e.g., higher memory usage).
        ### Example Template(Key Format Reference Only):
        ```
        ## Summary of the expert's tuning history
        **Task Objective**: Increase Nginx QPS by 20% under high concurrent load.
        **Progress Status**: 70% completed - 7% improvement achieved.
        0.  **Iteration 0**:
            - Tuning Concept: Not yet executed - initial baseline established
            - Core Params: No changes made
            - Operation Result: Baseline metric recorded at xxx
            - Key Finding: No tuning actions taken; initial performance benchmark established for future comparison
        1.  **Iteration 1**:
            - Tuning Concept: The memory usage rate is far from reaching its limit, and parameters related to memory utilization can be adjusted to increase it.
            - Core Params: {{memory=50GB, xx=true}}
            - Operation Result: QPS increased from 70507.0 to 129443.0 (+83.59% vs baseline, +83.59% vs prev).
            - Key Finding: Increasing the memory setting was correct, make memory params larger is better.
        ... (Additional numbered steps for subsequent actions)
    recommender:
      value: |
        You are a system application optimization expert. Your task is to leverage your extensive experience in optimization to provide relevant recommendations.
        ### Overall Target:
        - The performance metric for the scenario is **{performance_metric.name}**, which signifies: {performance_metric.value}. The goal of tuning optimization is to achieve {slo_goal:.2%}.
        - The system performance analysis report is as follows:{performance_analysis_report}
        - The related parameters available for analysis and their descriptions are: {params_set_str}
        """ + "{long_mem}"  + "{short_mem}" + """
        ### Guidelines:
        - "Clear": Ensure that the answers are clear, concise, and directly address the question.
        - **Cautious, Lazy-Type Modification**: Only output parameter changes expected to yield good results; for continuous parameters, the adjustment range should be between 0~50%, and for discrete/enumerated parameters, avoid extreme adjustment spans; do not adjust parameters that are analyzed in the context as having "no impact" on performance.
        - **Logical**: Parameter adjustments should align with the tuning strategy, with each adjustment having a corresponding logic, and rigorously adjust mutually exclusive parameters simultaneously.
        - "Strict Formatting": Must meet type and unit requirements; the default unit for numerical values is "bytes". If a unit follows the numerical value, it should be represented as a string (e.g., "512MB"). Parameter output must be in **JSON format**.
        ### Example Template:
        ```
            {{
                "tuning_concept": "The main bottleneck in the current scenario is IO, but the system's memory usage is only about 30% of the current capacity. The current memory parameter is set at 20G, which is advisable to appropriately increase to enhance memory resource utilization and thereby accelerate performance ... (Additional tuning_concept)",
                "PARAM": {{
                "memory": "30GB",
                ... (Additional params)
                }}
            }}
        ```
      meta: {lang: en, version: 1}
# ==================================================== mysql =====================================================
mysql:
  <<: *common
# ==================================================== pgsql =====================================================
pgsql:
  <<: *common
# ==================================================== redis =====================================================
redis:
  <<: *common
# ==================================================== spark =====================================================
spark:
  <<: *common
# ==================================================== flink =====================================================
flink:
  <<: *common
# ==================================================== ceph =====================================================
ceph:
  <<: *common
# ==================================================== gaussdb =====================================================
gaussdb:
  <<: *common